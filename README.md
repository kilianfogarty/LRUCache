# LRUCache
Educational project to implement an LRUCache in C.

I would like to implement Value Structs, TTL, and better hashing to reduce collisions. 
I would also like to make the hashtable change the singly linked lists into red black trees similar to Java's Hashmap implementation for practice.
